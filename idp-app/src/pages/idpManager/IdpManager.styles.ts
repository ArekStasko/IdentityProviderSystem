const IdpManagerContainer = {
    width: "100%",
    height: "100%",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
}

const IdpAlert = {
    mb: 4
}

export default {
    IdpManagerContainer,
    IdpAlert
}