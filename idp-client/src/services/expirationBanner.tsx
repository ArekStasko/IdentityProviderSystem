import React from "react";


interface ExpirationBanner {
    onExpiration: () => React.ReactNode;
}

const ExpirationBanner = ({onExpiration}: ExpirationBanner) => {

    return (
        <>
        </>
    )
}

export default ExpirationBanner